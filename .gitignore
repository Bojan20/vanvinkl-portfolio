# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
/coverage

# Next.js
.next/
/out/

# Production
/build
/dist

# Rust/WASM
target/
*.wasm
!public/wasm/*.wasm
Cargo.lock

# Misc
.DS_Store
._*
*.pem
Thumbs.db

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Environment
.env
.env*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea/
.vscode/
*.swp
*.swo

# Claude
.claude/

# Portfolio media assets — source files (not deployed)
audioSlotPortfolio/           # WAV source files (local backup)
videoSlotPortfolio/           # Source video files (pre-encode)
audioSlotPortfolio/*.wav

# Deployed media — tracked (needed for Vercel deploy via git)
# public/videoSlotPortfolio/ — tracked (MP4 videos + poster JPGs)
# public/audioSlotPortfolio/ — tracked (OPUS/M4A audio)

# Skip originals/backups
public/videoSlotPortfolio/*_ORIGINAL*
public/videoSlotPortfolio/._*
